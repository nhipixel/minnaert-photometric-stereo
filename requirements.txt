numpy>=2.0
scipy>=1.14
matplotlib>=3.9
pillow>=10.0
sympy>=1.13
pytest>=8.0

# Needed only for the DiLiGenT loader. Pillow does not reliably preserve
# 16 bit depth on multichannel PNGs.
opencv-python>=4.10
